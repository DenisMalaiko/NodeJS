# Lesson 1 — Node.js: архітектура, завдання та основні концепції

This project demonstrates a basic MSC architecture written in TypeScript using Node.js.

## 🚀 How to Run

1. Install dependencies: ```npm install```
2. Add: ```npm run habit -- add --name "<текст звички>" --freq <daily|weekly|monthly>```
3. List: ```npm run habit -- add list```
4. Done: ```npm run habit -- done  --id <ідентифікатор>```
5. Stats: ```npm run habit -- stats```
6. Delete: ```npm run habit -- delete --id <ідентифікатор```
7. Update: ```npm run habit -- update --id <ідентифікатор> --name "<текст звички>" --freq <daily|weekly|monthly>```